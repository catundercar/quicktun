// AuthService — operator login + session management.
syntax = "proto3";

package quicktun.v1;

import "google/api/annotations.proto";
import "google/api/field_behavior.proto";
import "google/protobuf/empty.proto";
import "google/protobuf/timestamp.proto";

import "quicktun/v1/operator.proto";

option go_package = "github.com/tulip/quicktun/gen/go/quicktun/v1;quicktunv1";

// AuthService authenticates operators and issues bearer session tokens.
service AuthService {
  // Login exchanges email+password for a bearer access_token.
  rpc Login(LoginRequest) returns (LoginResponse) {
    option (google.api.http) = {
      post: "/v1/auth:login"
      body: "*"
    };
  }

  // Logout revokes the caller's current session (or a specific session if
  // session_name is supplied — admin-only path).
  rpc Logout(LogoutRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {
      post: "/v1/auth:logout"
      body: "*"
    };
  }

  // WhoAmI returns the operator identified by the current bearer token.
  rpc WhoAmI(WhoAmIRequest) returns (WhoAmIResponse) {
    option (google.api.http) = {
      get: "/v1/auth:whoami"
    };
  }
}

message LoginRequest {
  string email    = 1 [(google.api.field_behavior) = REQUIRED];
  string password = 2 [(google.api.field_behavior) = REQUIRED];
}

message LoginResponse {
  string access_token = 1;
  google.protobuf.Timestamp expire_time = 2;
  Operator operator = 3;
}

message LogoutRequest {
  // Resource name of a specific session to revoke (admin-only).
  // Format: operators/{op}/sessions/{session}
  // Empty = revoke caller's current session.
  string session_name = 1;
}

message WhoAmIRequest {}

message WhoAmIResponse {
  Operator operator = 1;
}
